import React from "react";
import Navbar from "./Nav";
import NavMobile from "./NavMobile";

const Layout = () => {
  return (
    <div>
      <Navbar />
      <NavMobile />
    </div>
  );
};

export default Layout;
